%%[0
%include lhs2TeX.fmt 
%include afp.fmt
%%]

Auxiliary attribute that determines whether an expression is the last expression in one of the possible paths in a function body

%%[8
ATTR AllGrExpr [ hasNext: {Bool} | | ]

SEM GrBind
  | Bind     expr . hasNext = False

SEM GrExpr
  | Seq      expr . hasNext = True
             body . hasNext = @lhs.hasNext

SEM GrExpr
  | * - Seq Case Catch  loc . isLastExpr = not @lhs.hasNext
%%]
